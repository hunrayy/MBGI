.vote-component-container {
  background-color: #000;
  color: #fff;
  min-height: 100vh;
  overflow-x: hidden;
  margin-top: 80px;
}

/* 🔥 Flex layout for desktop */
.vote-page-content {
  display: flex;
  flex-direction: row;
  align-items: stretch;
  justify-content: center;
  gap: 3rem;
  padding: 3rem 6rem;
}

/* 🖼️ Candidate image section */
.candidate-image-section {
  flex: 1;
  height: 100vh;
  overflow: hidden;
  border-radius: 20px;
  position: relative;
  box-shadow: 0 0 25px rgba(255, 105, 180, 0.3);
}

.candidate-image {
  width: 100%;
  height: 100%;
  object-fit: contain;
  transition: transform 0.6s ease;
}

.candidate-image-section:hover .candidate-image {
  transform: scale(1.05);
}

/* 🧾 Candidate details section */
.candidate-details-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 1rem;
  padding: 2rem 1.5rem;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 20px;
  backdrop-filter: blur(10px);
  box-shadow: 0 0 25px rgba(255, 105, 180, 0.2);
}

.candidate-name {
  font-size: 2.5rem;
  font-weight: 800;
  /* color: #ffb6c1; */
  text-transform: uppercase;
  letter-spacing: 2px;
}

.candidate-info {
  font-size: 1.2rem;
  color: #ccc;
}

.candidate-bio {
  font-size: 1rem;
  color: #ddd;
  line-height: 1.6;
  margin: 1rem 0;
}

/* 🗳️ Vote stats */
.vote-stats {
  margin: 1rem 0;
  background-color: #222;
  padding: 1rem;
  border-radius: 10px;
  text-align: center;
}

.vote-count {
  font-size: 1.5rem;
  font-weight: bold;
  color: #ff69b4;
  text-shadow: 0 0 10px rgba(255, 105, 180, 0.7);
}

/* ✨ Glowing vote button */


/* RESPONSIVE LAYOUT */
@media (max-width: 992px) {
  .vote-page-content {
    flex-direction: column;
    padding: 1.5rem;
  }

  /* .candidate-image-section {
    height: 50vh;
  } */

  .candidate-details-section {
    margin-top: 2rem;
  }

  .candidate-name {
    font-size: 2rem;
  }
}







@media (max-width: 768px) {
  .vote-component-container {
    flex-direction: column;
  }

  .candidate-image {
    /* height: 70vh;  */
  }

  .candidate-details-box {
    text-align: center;
    padding: 1.5rem;/
  }

  .vote-btn {
    align-self: center;
  }
}












.vote-action-box {
  text-align: center;
  margin-top: 2rem;
  background: rgba(255, 255, 255, 0.05);
  padding: 2rem 1.5rem;
  border-radius: 20px;
  border: 1px solid rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  box-shadow: 0 0 25px rgba(255, 105, 180, 0.15);
}

.vote-heading {
  font-size: 2rem;
  font-weight: 800;
  color: #fff;
  letter-spacing: 1px;
  margin-bottom: 0.8rem;
}

.vote-subtext {
  font-size: 1.1rem;
  color: #ccc;
  margin-bottom: 1.8rem;
}

/* ✨ Glowing Vote Button */

.cast-vote-btn {
    background: linear-gradient(90deg, #ff1493, #ff6ec7);
  color: white;
  width: 100%;
  border: none;
  padding: 2rem 2.5rem;
  font-size: 1.1rem;
  font-weight: 700;
  border-radius: 30px;
  cursor: pointer;
  text-transform: uppercase;
  letter-spacing: 1px;
  transition: all 0.3s ease;
  box-shadow: 0 0 20px rgba(255, 105, 180, 0.4);
}

.cast-vote-btn:hover {
  transform: scale(1.05);
  box-shadow: 0 0 25px rgba(255, 105, 180, 0.9),
              0 0 45px rgba(255, 105, 180, 0.6);
}





.crown-icon-wrapper {
  font-size: 2.8rem;
  color: #ff6ec7;
  text-shadow: 0 0 10px rgba(255, 105, 180, 0.8),
               0 0 20px rgba(255, 105, 180, 0.5);
  animation: crownGlow 2s infinite alternate;
  margin-bottom: 1rem;
}

@keyframes crownGlow {
  from {
    text-shadow: 0 0 10px rgba(255, 105, 180, 0.7),
                 0 0 20px rgba(255, 105, 180, 0.4);
  }
  to {
    text-shadow: 0 0 20px rgba(255, 182, 193, 0.9),
                 0 0 40px rgba(255, 182, 193, 0.6);
  }
}


















/* 🏦 Payment Modal Overlay */
/* .payment-modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 200;
  animation: fadeIn 0.3s ease-in-out;
} */

.payment-modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  justify-content: center;
  /* Remove vertical centering here */
  overflow-y: auto; /* enable scrolling if modal is taller than viewport */
  padding: 2rem 1rem; /* spacing so modal doesn't touch edges */
  z-index: 200;
  animation: fadeIn 0.3s ease-in-out;
}

/* 💡 Modal Box */
/* .vote-component-payment-modal {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 20px;
  padding: 2.5rem;
  width: 90%;
  max-width: 500px;
  color: white;
  text-align: center;
  backdrop-filter: blur(20px);
  box-shadow: 0 0 25px rgba(255, 105, 180, 0.6);
  animation: popUp 0.4s ease;
  position: relative;
} */


.vote-component-payment-modal {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 20px;
  padding: 2rem;
  width: 90%;
  max-width: 500px;
  max-height: 90vh; /* prevent modal from exceeding viewport height */
  overflow-y: auto; /* scroll content inside modal */
  color: white;
  text-align: center;
  backdrop-filter: blur(20px);
  box-shadow: 0 0 25px rgba(255, 105, 180, 0.6);
  animation: popUp 0.4s ease;
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}




/* ❌ Close Button */
.close-modal-btn {
  position: absolute;
  top: 12px;
  right: 18px;
  background: transparent;
  color: #fff;
  font-size: 2rem;
  border: none;
  cursor: pointer;
  transition: color 0.3s;
}

.close-modal-btn:hover {
  color: #ff6ec7;
}

/* 🏦 Account Details */
.account-details {
  margin: 1.5rem 0;
  text-align: left;
  background-color: rgba(255, 255, 255, 0.07);
  padding: 1.5rem;
  border-radius: 12px;
}

.account-details p {
  margin: 0.5rem 0;
  font-size: 1.1rem;
}

.account-number {
  font-weight: 700;
  color: #ff6ec7;
}

/* 📋 Copy Button */
.copy-btn {
  display: block;
  width: 100%;
  background: linear-gradient(90deg, #ff1493, #ff6ec7);
  color: #fff;
  border: none;
  padding: 0.9rem;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 8px;
  cursor: pointer;
  margin-top: 1rem;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.copy-btn:hover {
  transform: scale(1.05);
  box-shadow: 0 0 20px rgba(255, 105, 180, 0.8);
}

/* ℹ️ Note */
.modal-note {
  margin-top: 1.5rem;
  font-size: 0.95rem;
  color: #ddd;
}

/* ✨ Animations */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes popUp {
  from {
    transform: scale(0.8);
    opacity: 0;
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}



























/* ===========================
   Step 1: User Info Form
   =========================== */
.user-info-form {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  text-align: center;
}

.user-info-form  {
  color: #ff6ec7;
  font-size: 1.8rem;
  margin-bottom: 1rem;
  letter-spacing: 1px;
}

.user-info-form input {
  width: 100%;
  padding: 0.8rem 1rem;
  font-size: 1rem;
  border-radius: 12px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  background: rgba(255, 255, 255, 0.05);
  color: #fff;
  outline: none;
  transition: all 0.3s ease;
  backdrop-filter: blur(10px);
}

.user-info-form input::placeholder {
  color: #ccc;
}

.user-info-form input:focus {
  border: 1px solid #ff6ec7;
  box-shadow: 0 0 15px rgba(255, 105, 180, 0.5);
}

.next-step-btn {
  background: linear-gradient(90deg, #ff1493, #ff6ec7);
  color: white;
  border: none;
  padding: 1rem 2rem;
  font-size: 1.1rem;
  font-weight: 700;
  border-radius: 30px;
  cursor: pointer;
  text-transform: uppercase;
  letter-spacing: 1px;
  transition: all 0.3s ease;
  box-shadow: 0 0 20px rgba(255, 105, 180, 0.4);
  margin-top: 1rem;
}

.next-step-btn:hover {
  transform: scale(1.05);
  box-shadow: 0 0 25px rgba(255, 105, 180, 0.9),
              0 0 45px rgba(255, 105, 180, 0.6);
}

.next-step-btn:disabled {
  background: rgba(255, 105, 180, 0.4);
  cursor: not-allowed;
  box-shadow: none;
}




/* Modal Crown */
.modal-crown-wrapper {
  font-size: 3rem;
  color: #ff6ec7;
  text-shadow: 0 0 10px rgba(255, 105, 180, 0.8),
               0 0 20px rgba(255, 105, 180, 0.5);
  animation: crownGlow 2s infinite alternate;
  margin-bottom: 1rem;
}

/* Heading */
.modal-vote-heading {
  font-size: 1.8rem;
  font-weight: 800;
  color: #fff;
  letter-spacing: 1px;
  margin-bottom: 0.5rem;
}

/* Underline */
.modal-underline {
  width: 100%;
  margin: 0.5rem auto 1rem auto;
  border: 0;
  height: 2px;
  background: linear-gradient(90deg, #ff1493, #ff6ec7);
  border-radius: 2px;
}

/* Candidate mini info */
.modal-candidate-info {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1rem;
}

.modal-candidate-image {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  object-fit: cover;
  border: 2px solid #ff6ec7;

    /* Fix shrink issue */
  display: block;        /* remove inline layout quirks */
  flex-shrink: 0; 
}

.modal-candidate-text p {
  margin: 0;
  color: #fff;
  font-size: 0.95rem;
}



.email-note {
  font-size: 0.85rem;
  color: #ccc;
  margin-top: -0.5rem;
  margin-bottom: 1rem;
  text-align: left;
}

.email-note span {
  color: #ff6ec7; /* highlight keywords if desired */
  font-weight: 500;
}





.payment-modal-overlay.hide {
  display: none;
}

.payment-modal-overlay.show {
  display: flex;
}
